.env

# ==========================================================
# Editor swap / temporary files
# ==========================================================
# Vim/Neovim swap files
# Log files created during container runs
# Temporary files
# Backup files from editors
*.swp
*.log
*.tmp
*.bak

# ==========================================================
# Environment files
# ==========================================================
# Main environment file (contains passwords & secrets)
# Any other env files, just in case
# Keep the public example (safe to commit)
.env
*.env
!env_simple.env

# ==========================================================
# Docker runtime data and cache
# ==========================================================
# Local overrides — developer-specific
# Avoid recursive inclusion
# Local Docker metadata (if any)
# Temporary build/output folders
docker-compose.override.yml
**/.dockerignore
**/.docker
**/tmp/

# ==========================================================
# Database seed and dumps
# ==========================================================
# Keep initialization script in repo
db-seed/*
!db-seed/10-prepare-seed.sh

# ==========================================================
# Backup repository
# ==========================================================
# Borg repo and backup archives (contains credentials)
# Keep folder structure only
backups/
!backups/.gitkeep

# ==========================================================
# Certbot generated files (certificates / keys)
# ==========================================================
# Certificates and private keys — sensitive
# Keep folder
# ACME HTTP challenge temp files
# Keep folder
certbot/conf/*
!certbot/conf/.gitkeep
certbot/www/*
!certbot/www/.gitkeep

# ==========================================================
# WordPress & MariaDB persistent data
# ==========================================================
db_data/
wp_data/

# ==========================================================
# Reverse proxy cache/logs
# ==========================================================
# Access/error logs
# Proxy cache files
reverse-proxy/logs/
reverse-proxy/cache/

# ==========================================================
# Safe files to keep under version control
# ==========================================================
!README.md
!LICENSE
!docker-compose.yml
!*.sh
!*.conf
!*.Dockerfile
!Makefile
!env_simple.env
