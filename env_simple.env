# === Timezone ===
TZ="America/Lima"

# === Domain and email for future TLS setup ===
LETSENCRYPT_EMAIL=admin@example.com
LETSENCRYPT_DOMAIN=wp.example.com

# === MariaDB / WordPress ===
MYSQL_ROOT_PASSWORD=change-me-root        # TODO: replace with a strong, unique password
MYSQL_DATABASE=wordpress
MYSQL_USER=wpuser
MYSQL_PASSWORD=change-me-user             # TODO: replace with a strong, unique password
MYSQL_HOST=db                             # <— added: service name of the DB in compose network
WP_TABLE_PREFIX=wp_
WP_DEBUG=false

# === Seed (DB bootstrap) ===
SEED_MODE=auto                            # auto | import | export
SEED_FILE=seed.sql                        # Seed path inside the project
SEED_GZIP=false                           # true -> use seed.sql.gz (import/export compressed)
WAIT_TIMEOUT=120                          # Seconds to wait for DB readiness
VERBOSE=false

# === Compose profiles ===
# core: db + wordpress + reverse-proxy (HTTP only)
# tls:  certbot for certificate issuance and renewals
# ops:  wpcli-cron and backup jobs
PROFILES=core,ops
# Optional if you prefer env-driven enabling instead of --profile flag:
# COMPOSE_PROFILES=core,ops

# === Backups (Borg) ===
# Local path: ./backups (mounted from compose)
BORG_REPO=/backups/repo
BORG_PASSPHRASE=change-me-pass            # TODO: replace with a strong passphrase
BACKUP_CRON="0 2 * * *"    # every day at 02:00

# === Optional variables ===
# WORDPRESS_SITEURL=https://wp.example.com
# WORDPRESS_HOME=https://wp.example.com
# NGINX_WORKER_PROCESSES=auto
# CERTBOT_STAGING=true     # useful when testing ACME staging environment

